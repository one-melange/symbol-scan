# SymbolScan — Tasks

Standing backlog for SymbolScan. This is the single source of truth for what's planned,
in progress, and done.

## Convention

- Sections by horizon: **Now** (actively being worked), **Next** (queued), **Later**
  (someday/maybe), **Done** (shipped).
- One line per item: `- [ ] (Txx) Short title — one-line why · file:line`
- When an item ships, check it off and move it to **Done** with the commit/PR ref.
- IDs (`Txx`) are stable — don't renumber. New items get the next free number.
- `(verified)` = confirmed against source. `(reported)` = surfaced by audit, line refs
  not yet verified.

---

## Now

- [ ] (T28) Local-LLM "explain symbol" (⌘E) — pair the Tree-sitter index with a small local coder model (Qwen2.5-Coder via llama.cpp) so a picked symbol can be explained in place. **v1 landed** (branch `local_qwen_code`): a new `LLM/` subsystem behind an `LLMClient` protocol seam — `PromptBuilder` (pure, doc-only prompt from existing `Symbol` fields), `OpenAIChatStream` (stateless OpenAI-compatible `/v1/chat/completions` SSE client; pure request-builder + line-parser are unit-tested over canned bytes), `LlamaServer` (an `actor` that lazily spawns + health-polls a bundled `llama-server`, reusing `RepoScanner`'s deadlock-safe `Process` handling) and `LlamaServerClient`. UI: `.explain` added to `PickerAction`, bound to **⌘E** (`OverlayWindow.performKeyEquivalent`), streaming into a new `ExplanationState` machine + scrollable pane in `SymbolPickerView`; the controller grows the overlay while the pane is open. Wired through `AppDelegate` (client owned there, `shutdown()` on terminate; guarded by the XCTest early-return so tests never spawn). 26 new tests across `PromptBuilderTests` / `OpenAIChatStreamTests` / `ExplainFlowTests` / `ExplainCancellationTests` / `LlamaServerTests` (fake `LLMClient` + injectable launcher/handle); 173 tests / 22 suites green. **Review hardening** (actor reentrancy + lifecycle): `LlamaServer` coalesces concurrent starts onto one memoized `Task` (a suspended `ensureRunning` could otherwise be re-entered and spawn a second server on the same fixed port, orphaning the first); the health poll propagates cancellation (`checkCancellation` + throwing `sleep`, not swallowed `try?`) so an aborted start can't spin to the 30s deadline; the launcher terminates the child it spawned on any failure/cancellation; shutdown moved to `applicationShouldTerminate` → `.terminateLater` → `await` → `reply` (a fire-and-forget `Task` in `willTerminate` may never schedule, leaking the process); `hide()` now cancels the in-flight explanation and all selection changes (including mouse hover) route through `select(_:)` so the pane can't show one symbol's answer under another's heading. **Turnkey packaging landed** (branch `llm_turnkey_packaging`, PR #27): (a) `scripts/fetch-llama.sh` vendors a pinned prebuilt `llama-server` (release b10375, macOS arm64, checksum-verified) — the executable + its exact dylib closure, flattened — into gitignored `SymbolScan/Vendor/llama/`; an Xcode "Bundle llama runtime" phase copies it into `Contents/Helpers/llama/` and dev-signs the nested Mach-O (`@loader_path` rpath means co-location is all that's needed; Metal shaders are embedded in libggml-metal). (b) `ModelDownloader` (+ pinned `ModelCatalog`: Qwen2.5-Coder-3B Q4_K_M, ~2 GB, sha256-verified, streaming checksum) and `ModelProvisioner` (@MainActor, eager first-launch background download) provision the GGUF into `LlamaServerLocator.defaultModelDirectory()`; ⌘E shows live `.preparing(…%)` progress if invoked mid-download, and the menu bar surfaces `ModelStatusCopy`. Verified end-to-end that the bundled `llama-server` runs from inside the built `.app`. **Still deferred:** Developer-ID **notarization** of the bundled 3rd-party binary (this PR is dev-sign-only, runs on the author's machine; distribution to other machines is a later PR). No Preferences pane — turnkey by design. **Follow-ups (context, not v1):** enrich the prompt with source snippet + enclosing type + outgoing calls (new Tree-sitter extraction — see the doc-only note). Tauri was considered and rejected (native app already; LLM is a backend concern) · `SymbolScan/SymbolScan/LLM/`, `UI/SymbolPickerView.swift`, `UI/SymbolPickerViewModel.swift`, `App/OverlayWindow.swift`, `App/AppDelegate.swift`, `SymbolScan.xcodeproj/project.pbxproj`

## Next

- [ ] (T27) Surface collected docstrings in the picker — T26 collects `Symbol.doc` but nothing renders it. Options: first-line tooltip via SwiftUI `.help` on `SymbolRow` (lowest-footprint, no layout change), or a truncated second subtitle line (revisit `rowHeight`/list sizing at `SymbolPickerView.swift:21,96`). Search/injection deliberately unchanged. Related to T12's "full-signature tooltip" · `SymbolScan/SymbolScan/UI/SymbolPickerView.swift`

## Later

### Tier 3 — Polish / hygiene

- [ ] (T9) Decide on `Capture/` *(verified)* — `OCREngine` + `ScreenCapture` (~140 LOC) have zero callers; wire up OCR-driven triggering or delete both files. (T14 added `OCREngineTests` for the one pure function, `extractSymbolQuery`, but the files still have no production callers) · `SymbolScan/SymbolScan/Capture/`
- [ ] (T11) TextInjector — add injection-failure fallback (clipboard); note the `& 0xFFFF` BMP truncation (low impact for ASCII symbols) · `SymbolScan/SymbolScan/Input/TextInjector.swift:22`
- [ ] (T12) Picker UX *(reported)* — stable row IDs instead of `\.offset`, VoiceOver labels on rows, full-signature tooltip (empty-state copy fixed as part of T3) · `SymbolScan/SymbolScan/UI/SymbolPickerView.swift`

## Done

- [x] (T26) Collect doc comments / docstrings during parsing — Tree-sitter now captures each symbol's leading documentation into a new optional `Symbol.doc` (a second `signature`-style metadata field; rides the Codable JSON cache untouched, cache v6→**7** so stale doc-less caches rescan). Scope: *adjacent comment block* — Python uses the body docstring (first-statement string), every other grammar uses the contiguous leading comment block directly above the declaration with a **godoc adjacency rule** (a blank line ends the block, so an unrelated file-header comment isn't attached). New pure `docComment(...)` helper in `TreeSitterParser` (IO-free, unit-testable like `signature`): resolves a per-capture **anchor** (usually `nameNode.parent`, but Go `type_spec`→`type_declaration`, TS/JS `arrowfn`→enclosing declaration, and TS/JS climbs past `export_statement`), walks `previousNamedSibling` collecting per-grammar comment nodes (`comment`/`multiline_comment` Swift, `line_comment`/`block_comment` Rust, `comment` elsewhere) while **stepping over** a Rust `#[attr]` prelude, then `stripCommentMarkers` (`///`/`//!`/`//`, `/** */`/`/* */` + interior `*`) + PEP 257-style dedent + 1000-char cap. Gotcha found + fixed: tree-sitter-rust's `line_comment` range includes the trailing `\n`, so the initial trim needs `.whitespacesAndNewlines`, not `.whitespaces`. *Collect-only* — no picker/injection change yet (see T27); `injectionText` deliberately excludes `doc`, and `SymbolMatcher`/`Indexer.normalize` are untouched (they read `name`/key only). 11 new per-language extraction tests + adjacency/undocumented cases in `TreeSitterParserTests`, plus a `doc` round-trip in `IndexCacheTests`; 147 tests across 17 suites green · `SymbolScan/SymbolScan/Index/Symbol.swift`, `Index/TreeSitterParser.swift`, `Index/SymbolIndex.swift`, `SymbolScanTests/TreeSitterParserTests.swift`, `IndexCacheTests.swift` · branch `docstrings_detection`
- [x] (T23) Incremental cache: coalesced + proportional writes — T4's per-save incremental (`Indexer.reindexFiles`) re-serialized and atomically rewrote the **whole** `IndexCache` (O(total symbols)) on every debounced FSEvents batch — a multi-MB write per save on a large repo, even for a one-file change. Replaced the single `<hash>.json` with a **base snapshot + append-only patch log** (`<hash>.log`, cache v5→**6**): each increment appends one small `Patch { paths, symbols }` line (write ∝ change, not repo size), and `Indexer.loadCache` reconstructs by replaying the log over the base. Writes are also **coalesced** — `reindexFiles` now returns `(symbols, patch)` and does no IO; `SymbolIndex` buffers patches and flushes on a ~2s debounce (`incrementalFlushDelay`), so a save burst is one appended write. The log is bounded by **compaction** (full base rewrite + log clear) on full reindex and once it passes `maxJournalPatchesBeforeCompaction` (200); repo-switch and `applicationWillTerminate` synchronously append any queued patches and drain the IO queue (they do **not** compact) so a switch-back or a quit mid-debounce doesn't lose them. A new pure `Indexer.normalize` (ordering + derived `.directory` + dedup, shared by `buildIndex`/`reindexFiles`/load) and `applyPatches` guarantee replayed cache == full-scan ranking. Correctness stays in memory always; the journal is a self-healing cache (worst case after a crash is ~2s of stale on-disk entries, fixed on next save or ⌘R). No `project.pbxproj` edit. **Review hardening** (concurrency/durability): all cache IO (base write, log append/clear, reads) is serialized through one `ioQueue`, and every authoritative `compact` bumps a per-repo write **generation** so a stale in-flight incremental write scheduled before a full reindex is dropped rather than landing on top of the fresh base (fixes the full-scan-vs-journal write race). `finishFlush` no longer strands a newly-switched repo's patches or mutates the wrong repo's journal counter; and `appendPatches` separates a crash-torn final line so the next record can't fuse onto it. The steady-state flush enqueues its write **synchronously** onto the IO queue (via `enqueueAppend`/`enqueueCompact`, not a detached task whose dispatch has a gap), so repo-switch and termination can call `IndexCache.drain()` — a real barrier that guarantees any in-flight flush is durable and ordered before the repo is reloaded or the process exits (fixes the lost-in-flight-batch and switch-back-reads-stale races). Base + patch log + patch count are read as **one atomic `IndexCache.loadSnapshot`** (a separate `load`/`loadPatches` could straddle a `compact` and mix generations), and activation defers to an authoritative full scan via a `publishEpoch` guard **and** a `jobs[root] == nil` check (a scan that finishes *or is still running* mid-load isn't clobbered by the older cache snapshot). `normalize` also **sorts within each group** (code by file/line/name/kind, files/dirs by path) so a multi-file incremental batch — which iterates a `Set` — lands in the same canonical order as a full scan, keeping empty-query / equal-score ranking deterministic. New `IndexCacheTests` (patch codec/append/torn-line/torn-tail-append/clear/compact/generation-supersession/drain-barrier/atomic-snapshot) + `IncrementalIndexTests` (`(symbols, patch)` contract, patch-replays-on-load, multi-file-batch canonical order, and the linchpin `replayedLogEqualsAFullScanOfTheFinalTree`); 138 tests across 17 suites green · `SymbolScan/SymbolScan/Index/SymbolIndex.swift`, `Index/Indexer.swift`, `App/AppDelegate.swift`, `SymbolScanTests/IndexCacheTests.swift`, `IncrementalIndexTests.swift` · branch `incremental_cache_update`
- [x] (T24) Launch outside Xcode — the app previously only ran when launched from Xcode. Two halves. (1) **Open at Login** menu-bar toggle backed by `SMAppService.mainApp` (macOS 13+) — a new pure-ish `LoginItem` helper embedded in `AppDelegate.swift` (no `project.pbxproj` edit, same convention as `StatusItemController`), with a live checkmark re-read on every `menuNeedsUpdate`. (2) `scripts/install.sh` builds Release and installs `SymbolScan.app` into `/Applications` (quitting any running copy first) so it launches from Spotlight/Finder and a login item points at a **stable** path. Also set `INFOPLIST_KEY_LSUIElement = YES` on the app target so it starts straight into the menu bar as an agent (no dock-icon flash at login) rather than relying solely on the runtime `setActivationPolicy(.accessory)`. Documented the key caveat in code + docs: a login item registered from a DerivedData/Xcode build path won't relaunch, so Open-at-Login is meaningful only for the installed `/Applications` copy. README gained an "Install it (run outside Xcode)" section; DEVELOPMENT.md an "Install / run outside Xcode" section · `SymbolScan/SymbolScan/App/AppDelegate.swift`, `SymbolScan.xcodeproj/project.pbxproj`, `scripts/install.sh`, `README.md`, `DEVELOPMENT.md` · branch `incremental_cache`
- [x] (T25) App icon — the `AppIcon.appiconset` was an empty placeholder (all 10 slots unfilled). Added a designer-provided icon (metallic `( )` blades on an indigo squircle) as a single **1024×1024** `icon.png` in the appiconset. Uses the macOS **single-size** format: actool rejects a true one-entry universal/`mac`-1024 icon ("unassigned child"), but a single 1024 assigned to the `mac` `512pt@2x` slot compiles cleanly, so the whole icon is one file to maintain (macOS downscales it for smaller displays; per-size PNGs can be re-added if a hand-tuned small size is ever needed). Two gotchas found + fixed: (a) the icon must land in the **tracked** catalog `SymbolScan/SymbolScan/Assets.xcassets` — an untracked stray `SymbolScan/Assets.xcassets` also existed and edits there compiled to nothing (`CFBundleIconName` stayed empty); (b) the source PNG needs **transparent corners** (macOS doesn't mask the icon) — verified alpha=0 at all four corners. Verified end-to-end: a clean build embeds `Assets.car` + `AppIcon.icns` and sets `CFBundleIconName = AppIcon`; the rendition extracted from the built bundle matches the art · `SymbolScan/SymbolScan/Assets.xcassets/AppIcon.appiconset/icon.png` · branch `incremental_cache`
- [x] (T13) Deployment target lowered 26.0 → 14.0 — `MACOSX_DEPLOYMENT_TARGET` was macOS 26 (Tahoe-only) across all four configs (app + test, Debug + Release), the Xcode-new-project default from a Tahoe machine rather than a real floor: nothing in the shipping code needs it — the single availability guard anywhere is `#available(macOS 14.0)` in the dead `Capture/ScreenCapture.swift`, and `SMAppService` (T24) only needs 13.0. Set to 14.0 (Sonoma) — matches that guard and widens the audience by three OS generations. Verified: a full clean build **and** `xcodebuild test` (125 tests, 17 suites) green at `arm64-apple-macos14.0`; the built `Info.plist` reports `LSMinimumSystemVersion = 14.0`. CI stays pinned to `macos-26` (a newer runner still builds an older deployment target) · `SymbolScan/SymbolScan.xcodeproj/project.pbxproj` · branch `incremental_cache`
- [x] (T14) Test gaps — added the feasible coverage. `HotkeyMatcherTests` pins the full trigger matrix from T6 (default pass-through, exact-modifier discrimination, rebind-suppresses, `naturalMarker`); `HotkeyPreferenceTests` covers the `UserDefaults` round-trip, `decode` fallbacks, and `displayString`/`keyLabel`; `OCREngineTests` covers `OCREngine.extractSymbolQuery` — the one pure function in the (otherwise dead, see T9) `Capture/`. `EventTap` and the settings UI became testable precisely because T6 pushed all decision logic into pure functions; the residual CGEventTap callback, `KeyRecorderNSView` responder, and `NSWindow` activation remain not-unit-testable and are documented as such. `AppDelegate`/`OverlayWindow` had **no further** pure logic to extract — already covered by `StatusMenuModel`/`OverlayPlacement`/`InjectionComposer`. 127 tests across 17 suites green · `SymbolScanTests/HotkeyMatcherTests.swift`, `HotkeyPreferenceTests.swift`, `OCREngineTests.swift`, `InjectionComposerTests.swift` · branch `polish`

- [x] (T6) Configurable hotkey — the trigger keys are now user-rebindable with a live UI, replacing the hardcoded `@`/`#`/`⌘⇧O` branches. Introduced a data-driven, **pure** model in `EventTap.swift`: `HotkeyBinding` (keyCode + a `Codable` `HotkeyModifiers` OptionSet), a `TriggerAction` (`claudeAt`/`codexHash`/`openSymbol`, each with a fixed injected `marker`) that replaces the old `Trigger` enum's semantic role, and `HotkeyMatcher.match` — the seam that makes the CGEventTap callback a thin wrapper and the whole matrix unit-testable. Matching is **exact modifier-set equality** (not the old `contains` ladder), so ⌘⇧2 no longer incidentally fires the bare-⇧ `@` binding; the legacy ctrl-variant branches were dropped. The **pass-through rule is the crux**: `passThrough == (naturalMarker(binding) == action.marker)`, so the default `@`/`#` (which literally type their marker) pass the keystroke through **exactly as before**, while ⌘⇧O and any rebind to a non-typing combo suppress and inject the marker as a prefix — `InjectionComposer` was migrated from `(trigger)` to `(action, markerAlreadyTyped)` to derive the prefix from that bit (verified byte-identical at defaults, incl. the `@@`-regression). Persistence mirrors `RepoPreference`: `HotkeyPreference` stores one JSON blob under `SymbolScan.hotkeyBindings` with an injectable `UserDefaults` and a pure `decode` that falls back to `.defaults` on missing/corrupt data. The UI (a `HotkeySettingsView` + an `NSViewRepresentable` `KeyRecorderView` modeled on `SearchFieldRepresentable`) is hosted by a hand-managed `PreferencesWindowController` (not the awkward-to-open SwiftUI `Settings` scene) opened from a new **Hotkeys…** menu-bar item; it reuses the overlay's `NSApp.activate` dance so the recorder can take key focus. Live reload is a direct `EventTap.updateBindings` (main-thread only — the callback runs on the main run loop, documented, no lock), and `EventTap.recordingSuspended` stops a still-bound combo popping the overlay mid-capture. All decision logic is pure and tested; the CGEventTap, the recorder responder, and the window activation are honestly left to manual verification. **All new types embedded in existing files → zero `project.pbxproj` edits.** New `HotkeyMatcherTests` + `HotkeyPreferenceTests`. **Follow-up (post-review):** collapsed the three per-tool triggers (`@`/`#`/⌘⇧O) to a **single** configurable hotkey — `TriggerAction`/`HotkeyBindings` removed, `HotkeyPreference` now stores one `HotkeyBinding` (default ⌘⇧O, a *suppressed* modifier combo), the marker is a `HotkeyMatcher.marker` constant, and the settings pane is one row. Two UX fixes shipped with it: a persistent **"Saved" confirmation** in the settings pane (recording alone didn't make it clear the change stuck), and a **reliable focus-grab** (activate-before-key in `OverlayWindowController.show` + a first-responder retry in `SearchFieldRepresentable`) that fixes the cursor staying stuck in the previously-focused app — notably iTerm, where the old passed-through `@` let the terminal keep focus; the suppressed default combo avoids that entirely · `SymbolScan/SymbolScan/Input/EventTap.swift`, `App/OverlayWindow.swift`, `App/AppDelegate.swift`, `UI/SymbolPickerView.swift`, `SymbolScanTests/HotkeyMatcherTests.swift`, `SymbolScanTests/HotkeyPreferenceTests.swift`, `SymbolScanTests/InjectionComposerTests.swift` · branch `polish`
- [x] (T10) Logging — replaced all 17 `print()` calls with a central `os.Logger` namespace (`enum Log` in `AppDelegate.swift`, categories app/input/index/search/scanner/parser/notifications keyed on the bundle id). **Deleted** the per-keystroke keycode log at the top of `EventTap.handle` outright (a privacy leak — it logged every key the user typed, system-wide), rather than migrating it. User-identifying interpolation is marked `privacy: .private` — the search query (`SymbolIndex.search`, still `#if DEBUG`) and the repo path (`RepoScanner.gitLsFilesRaw`); counts and repo last-path-components stay `.public`. Each call-site file gained `import os` (the `privacy:` interpolation is unavailable otherwise, and it autocloses captured values so `RepoScanner` needed explicit `self.root`) · `SymbolScan/SymbolScan/App/AppDelegate.swift`, `App/IndexNotifier.swift`, `Input/EventTap.swift`, `Index/SymbolIndex.swift`, `Index/RepoScanner.swift`, `Index/TreeSitterParser.swift` · branch `polish`
- [x] (T15) Cleanups — the three documented magic numbers are now named constants with a why-comment: `TextInjector.defaultInjectDelay` (0.05s overlay-resign settle), `OverlayWindowController.focusHandbackDelay` (0.12s for the previous app to regain key before injection — the paired `inject(after: 0)` is explained), and `SymbolMatcher.defaultResultLimit` (10; the two "top 10" docstrings now reference it, default kept 10 so the count-asserting tests stay green). The "0.62 Y-position" and "hoist regex to `static let`" clauses were already moot (retired by `OverlayPlacement` / `RegexParser` deletion) · `SymbolScan/SymbolScan/Input/TextInjector.swift`, `App/OverlayWindow.swift`, `Index/SymbolIndex.swift` · branch `polish`

- [x] (T4) Incremental reindex on save — a new `RepoWatcher` (single FSEvents stream, `FileEvents`+`NoDefer`+`WatchRoot`, 0.3s coalescing latency, dispatch-queue scheduled) watches the active repo and drives a **per-file** splice, so a file an editor/agent writes becomes searchable in ~1s with no manual Reindex. FSEvents, not a `DispatchSource` vnode source: the latter needs an FD per path and can't see *newly-created* files — exactly the brand-new untracked file we care about. New `Indexer.reindexFiles(_:root:existing:cacheBase:)` re-parses only the changed paths and splices them into the existing array: it drops each changed file's old code+`.file` symbols, re-adds those that still pass a **full-scan membership** predicate (exists && !symlink && !`isExcluded` && !gitignored), recomputes `.directory` entries from the resulting file set, and re-establishes the code-before-file/dir ordering + string-key dedup so ranking is identical to a full build. gitignore consistency comes from a new `RepoScanner.ignored(_:)` batching one `git check-ignore -z --stdin` per drain (same drain-before-wait deadlock guard as `gitLsFilesRaw`; exit 1 = "none ignored", not error). `SymbolIndex` gained a single-flight drain (`filesChanged`/`rescanRequested`/`drainIncremental`/`finishIncremental`) that snapshots `symbols` on the main actor, reparses off-main, and writes back only if the repo is still active and no full `jobs[root]` is running — incremental updates are **silent** (no `IndexNotifier` banner; only ⌘R / repo-switch notify). Untracked files were already enumerated (`git ls-files --others`); the gap was purely that nothing *re-triggered* indexing. `AppDelegate` owns + retargets the watcher at the 3 activation sites (behind the existing XCTest guard). Dropped/coalesced events or a moved root → `.rescan` → full reindex. `IndexCache.version` 4→5. New `IncrementalIndexTests` (add/modify/delete, untracked pickup, gitignored-not-indexed, oversize `.file`-only, ordering/dedup, cache round-trip); FSEvents itself left untested (flaky) since all decision logic is pure · `SymbolScan/SymbolScan/Index/RepoScanner.swift`, `Index/Indexer.swift`, `Index/SymbolIndex.swift`, `App/AppDelegate.swift`, `SymbolScanTests/IncrementalIndexTests.swift` · branch `incremental_reindex`
- [x] (T7) RepoScanner robustness — three fixes. (a) **Excluded dirs on the git path:** `git ls-files` only honours `.gitignore`, so a repo that *commits* its build output got it indexed. Added a pure `RepoScanner.isExcluded(_:)` (exact path-**component** match, so `vendor.min.js` the file is untouched while `vendor/` the dir is pruned) applied at the shared `gitLsFilesRaw` choke point (both `gitLsFiles` and `enumerateAllFiles` inherit it), and expanded `excludedDirs` with `target`/`vendor`/`__pycache__`/`.next`/`dist`. `isExcluded` is reused by the T4 watcher filter + membership predicate. (b) **File-size cap:** `SymbolParser.parse(url:)` now skips files over `maxParseFileSizeBytes` (2 MB) via a pre-read `.fileSizeKey` check — cheaper than reading a multi-MB blob into a `String` just for `isMinified` to reject it; the file still gets a searchable `.file` entry. (c) **Symlinks:** the FileManager walk skips symlinked dirs (`.isSymbolicLinkKey` + `skipDescendants`) so enumeration can't escape the repo or loop. Core regression `gitPathExcludesCommittedBuildDirs` (real git repo) + `isExcluded`/`ignored`/symlink/oversize tests; the `RepoScannerTests` minified-name pins still pass (exact-component match) · `SymbolScan/SymbolScan/Index/RepoScanner.swift`, `Index/TreeSitterParser.swift`, `Index/Symbol.swift`, `SymbolScanTests/RepoScannerTests.swift` · branch `incremental_reindex`
- [x] (T22) `Indexer.buildIndex` testability seam + `IndexerTests` — the scan → parse → dedup → cache composition had no test (deferred by T19). The seam turned out to be **one defaulted parameter**, not protocol injection: `IndexCache.save`/`load` already took an injectable `base:`, `Indexer` just never forwarded it, so the only untestable dependency was the cache write landing in the user's real Application Support dir. `buildIndex(root:cacheBase:)`/`loadCache(root:cacheBase:)` now forward it; the default is evaluated per call site, so both production call sites still pass `(root:)` and behave identically. Tests run against a **real temp git repo** with real grammars and real IO on purpose — mocking the scanner would only re-test units that are already covered, and it was precisely a whole-pipeline run that exposed the T8 minified-bundle bug when every individual unit was behaving correctly. 12 tests: per-dialect symbols in one pass, Go receiver signature survival, repo-relative paths (T2), the minified bundle yielding no code symbols but keeping its `.file` entry, T18 file/dir entries, the documented code-symbols-before-paths ordering, `fileCount` counting source files only, dedup, cache round-trip landing **only** in the injected base, `notGitRepo`, and cancellation. `makeTempDir`/`runGit` hoisted out of `RepoScannerTests` into a shared `TestSupport.swift` (third user). All verified by mutation — dropping the `cacheBase` forwarding, the `isMinified` guard, or the append ordering each turns the suite red · `SymbolScan/SymbolScan/Index/Indexer.swift`, `SymbolScanTests/IndexerTests.swift`, `SymbolScanTests/TestSupport.swift` · branch `multi_lang`
- [x] (T8) Language coverage — JS/JSX closed. `.js`/`.jsx`/`.mjs`/`.cjs` now index via a new `.javascript` dialect backed by `tree-sitter-javascript` (exact 0.23.1, matching the sibling grammars' vintage). `.jsx` needs no separate grammar — unlike TS/TSX, the JavaScript grammar parses JSX natively. The JS query is deliberately **not** a copy of the TS one: JS names classes with `(identifier)` where TS uses `(type_identifier)`, and has no `interface_declaration`/`type_alias_declaration`/`abstract_class_declaration`, so a copied query would fail to compile and (post-T21) silently zero out every `.js` file. Minified/generated JS is screened out in two layers, because `git ls-files` doesn't honour `RepoScanner.excludedDirs`: a cheap name check (`*.min.js`, `*.bundle.js`) in `Language.detect` that avoids the read entirely, and — since name patterns miss the common case, Vite emits `index-DJ7HgGZS.js` — a `SymbolParser.isMinified` content check that skips any source whose longest line exceeds 5,000 characters. Verified against a real repo: two committed 4.9 MB Vite bundles were contributing **16,901** junk symbols (`ur`, `Rr`, `Sm`, …); measured across 174 real files, hand-written code topped out at 1,289 chars/line vs 628,095 for minified, so the threshold isn't delicate. A general file-size cap is still T7. `project.pbxproj` needed the package in **all five** sections (`PBXBuildFile`, frameworks phase, target `packageProductDependencies`, project `packageReferences`, and the two `XC*` blocks); the middle two are the easy ones to miss and fail differently · `SymbolScan/SymbolScan/Index/TreeSitterParser.swift`, `Index/Symbol.swift` + build · branch `multi_lang`
- [x] (T20) `.tsx` parsed with the TSX grammar — `.tsx` was routed to `Language.typescript`, whose grammar can't parse JSX. Tree-sitter error-recovers rather than failing, so no fallback ever fired and symbols in every `.tsx` file silently vanished from the index (pinned by `jsxUnderThePlainTypeScriptGrammarLosesSymbols`). Added a `.tsx` dialect mapped to `tree_sitter_tsx()`. **No project-file change was needed**: `TreeSitterTSX` is a second *target* inside the already-linked `TreeSitterTypeScript` *product*, so `import TreeSitterTSX` just works (adding an `XCSwiftPackageProductDependency` for it would be invalid — no such product). TSX names every node/field we match identically to TypeScript, so the query text is shared but compiled/cached separately (a `Query` is bound to a `TSLanguage`). `Language` is now documented as the **grammar key**, not a language name — nothing renders or persists it (`Symbol` has no language field), so raw values are free to change; dead `FileEntry` and `Language.extensions` deleted. `IndexCache.version` 3→4 so tsx-symbol-free caches aren't served forever · `SymbolScan/SymbolScan/Index/TreeSitterParser.swift`, `Index/Symbol.swift`, `Index/SymbolIndex.swift` · branch `multi_lang`
- [x] (T21) Removed the dead `RegexParser` fallback — `TreeSitterParser.parse` returns nil only when a grammar or query fails to *build*, which is deterministic and cached-on-success, and tree-sitter error-recovers so a broken source file yields `[]` not nil. The fallback was therefore unreachable in production: deleted `Index/RegexParser.swift` (201 lines) + `RegexParserTests.swift` (124). `SymbolParser.parse` is now non-optional and reports a grammar-build failure **once per language** — silently returning `[]` is exactly how T20 went unnoticed. `TreeSitterParser.parse` stays optional: it's the `try #require` test seam and the only signal separating "broken grammar" from "no declarations". The one capability regex had that Tree-sitter didn't — `Symbol.signature`, still rendered by `SymbolPickerView` — was reimplemented for Go methods from `method_declaration`'s `receiver` field, so `(*Server) Close` and `(Conn) Close` are distinguishable in the picker. New `everyDialectBuildsAGrammarAndQuery` test guards the now-fallback-free path · `SymbolScan/SymbolScan/Index/TreeSitterParser.swift` · branch `multi_lang`
- [x] (T19) Background / non-blocking indexing + multi-repo + completion notification — root cause of the large-repo freeze was a `Process`/`Pipe` deadlock in `RepoScanner.gitLsFilesRaw`: it called `waitUntilExit()` *before* draining stdout, so a repo whose `git ls-files` output exceeded the ~64KB pipe buffer (a large monorepo of tens of thousands of files, well over 1 MB of output) blocked git on a full pipe while the app blocked waiting for git — a hang that never completed. Fixed by reading stdout to EOF before waiting + discarding stderr to `nullDevice`. Indexing also moved off the `@MainActor` into a nonisolated `Indexer.buildIndex` (`Task.detached`); `SymbolIndex` now holds an active-repo view plus a per-repo background job registry (`jobs: [URL: Task]`) so repos index concurrently and switching never cancels another repo's job (publishes guarded by `indexedRepoRoot`). Real-scan completion posts a `UNUserNotificationCenter` banner (`IndexNotifier`) + live menu-bar tooltip; cache hits stay silent. `activateRepo`/`reindex` are synchronous non-blocking launchers; dead-repo cleanup via `SymbolIndex.onRepoInvalid`; removed the dead `reindexFile`/`scanner` (see T4). Regression test `RepoScannerTests.enumerateAllFilesDoesNotDeadlockOnLargeGitOutput` (real repo, >64KB output — hangs the run if the deadlock returns). **Verified live** on a large monorepo (tens of thousands of symbols, UI responsive). Not yet tested: the job-manager concurrency (the `Indexer.buildIndex` composition half was closed by T22) · `SymbolScan/SymbolScan/Index/Indexer.swift`, `Index/SymbolIndex.swift`, `Index/RepoScanner.swift`, `App/IndexNotifier.swift`, `App/AppDelegate.swift` · branch `tree_sitter`
- [x] (T16) Tree-sitter parsing — replaced the regex extractor with real Tree-sitter grammars (Swift, Python, TypeScript, Rust, Go) behind a new `TreeSitterParser` + `SymbolParser` facade that keeps the `parse(source:language:path:)` boundary and falls back to `RegexParser` on any grammar/parse/query failure. Method-vs-function is now decided by node ancestry (not indentation); Go struct/interface/alias and Swift class/struct/enum/actor/protocol are disambiguated from the parse tree. Five grammar SwiftPM packages added to `project.pbxproj` (Swift pinned by revision to the `-with-generated-files` tag; others by exact version) and linked **statically** (no dylib signing); removed the machine-specific `HEADER_SEARCH_PATHS`. `IndexCache.version` bumped 2→3. `RegexParser` retained as fallback + its tests · `SymbolScan/SymbolScan/Index/TreeSitterParser.swift` + build · branch `tree_sitter`
- [x] (T17) Path injection — picking a **code symbol** now injects the file reference plus the name as `@<relativePath>:<line> <name>` (e.g. `@Index/SymbolIndex.swift:105 search`) instead of the bare name, consumed by `SymbolPickerViewModel.selectedInjectionText()` and `OverlayWindowController.confirmAndHide` (used for both inject and Tab-copy). **Follow-up fix:** the `@` was double-injected (`@@`) because the `@`/`#` triggers pass their keystroke through to the target app — so `Symbol.injectionText` now returns the reference *body* only and the leading marker comes from the trigger (nothing for `@`/`#`, a supplied `@` for `⌘⇧O`) via the pure `InjectionComposer`. Both covered by `LanguageAndSymbolTests` + `InjectionComposerTests` (incl. the exact `@@` regression) · branch `tree_sitter`
- [x] (T18) Directory and file recognition — every git-tracked file (any type) and every directory is now indexed as a searchable/injectable entry (`SymbolKind.file` / `.directory`); a file injects `<filename> <parent dir>` and a directory injects `<dir name> <parent dir>`. `RepoScanner.enumerateAllFiles()` (unfiltered git ls-files / FileManager walk) + `RepoScanner.directories(for:)` feed `SymbolIndex.reindex`; `IndexCache.version` bumped 1→2 to drop stale caches. Also fixed a latent `relativePath` symlink-aliasing bug (`/var`↔`/private/var`) · branch `tree_sitter`

- [x] (T1) Hardcoded repo path — removed; the active repo is persisted in `UserDefaults` (`RepoPreference` in `Index/RepoScanner.swift`) and restored on launch, with stale/deleted paths cleared automatically · branch `t3_t1_impl`
- [x] (T3) Repo selection + reindex UI — menu-bar status item (Choose Repo… / Reindex / recent repos / Quit) plus ⌘O / ⌘R / ⌘Q in the overlay; `NSOpenPanel` directory picker; truthful empty state (old copy referenced a nonexistent icon); occlusion warning when the icon is hidden by a full menu bar/notch; overlay now centered + top-pinned on the focused screen (multi-monitor fix, `OverlayPlacement`) · branch `t3_t1_impl`
- [x] (T5) Index persistence — pulled into T3: per-repo JSON cache (`IndexCache` in `Index/SymbolIndex.swift`) under `~/Library/Application Support/SymbolScan/index/`, loaded on activation so repo switches and relaunches skip the rescan; refresh is manual via Reindex (⌘R) · branch `t3_t1_impl`
- [x] (T2) Path-truncation / dead `relPath` — the initial index stored a bare filename instead of the repo-relative path. Fixed by giving `RegexParser.parse(url:language:relativePath:)` an explicit relative path and threading the already-computed `relPath` in at both call sites; dropped `reindexFile`'s now-redundant re-stamp · `SymbolScan/SymbolScan/Index/SymbolIndex.swift` + `Index/RegexParser.swift` (branch `t2_fix`)
